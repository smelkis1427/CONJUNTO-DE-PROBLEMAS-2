from typing import List
variable = (['jtref', 'sdjowkd', 'sdffncvvdda'])
def _var_(datos: List[str]) -> int:
    con = 0
    for variable in datos:
        if len(variable) > 2 and _var_[0] == _var_[1]:
            con += 1
        return con
